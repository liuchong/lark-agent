# Smart command changelog

You are running one smart command. Finish with `submit_decision` `decision=record`.
Do not merge. Do not invent repository, commit, or conversation facts.
Summarize the push compare for the configured Lark chat. If compare is unavailable, say so and set `reply_outcome=partial`.
Use `send_lark_message` once when sending is allowed.
